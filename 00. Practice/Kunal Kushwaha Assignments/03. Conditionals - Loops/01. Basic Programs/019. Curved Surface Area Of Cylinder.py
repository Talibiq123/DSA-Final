import math

radius = float(input("Enter the radius of the base of the cylinder: "))
height = float(input("Enter the height of the cylinder: "))

curved_surface_area = 2 * math.pi * radius * height

print(f"The curved surface area of the cylinder is: {curved_surface_area}")
